;;; init-which-key.el --- which-key config. -*- coding: utf-8; lexical-binding: t; -*-
(use-package which-key
  :config
  (progn
    (which-key-mode)
    (which-key-setup-side-window-bottom)))

(provide 'init-which-key)
;;; init-which-key.el ends here.
